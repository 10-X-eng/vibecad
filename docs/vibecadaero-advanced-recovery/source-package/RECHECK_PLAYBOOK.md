# Recheck Playbook — Pass 04+

1. Never overwrite Pass 03. Create `VibeCADAero_Reconciliation_Pass_04_<shortsha>/`.
2. Freeze exact current `halthinks/vibecad` `main` SHA.
3. Compare from `df07a5e82ec2fb31515e10b33822253d69d496ff` to the new SHA.
4. Inspect all changes under:
   - `src/Mod/VibeCADAero/`
   - Native state/dispatch/preview control/commands
   - agent control and `/v1/aero`
   - detached job/solver execution
   - evidence/artifact/measurement semantics
   - intent memory
   - build/packaging/tests/docs.
5. Search for new/generic host Analysis Runtime work first. **Do not extend `AeroJobStore` into production authority.** Reconcile any upstream generalization against the Correction-01 host-runtime contract and preserve newer host work.
6. Recheck FluidX3D/CfdOF/Kaggle/Gmsh current source/API/license status.
7. Preserve component-specific third-party notice behavior. Do not invent product-wide licensing profiles or use-purpose enforcement.
8. Re-run contradiction searches for: `non-commercial VibeCAD`, `commercial profile`, `I agree`, `license gate`, `eventually`, `full Leishman`, and stale pass numbers.
9. Run bare package validation from the documented working directory.
10. Produce `DIFF_FROM_PASS_03.md`, updated ledger/bugs/traceability/manifest.
11. Do not write upstream unless the user separately and explicitly authorizes implementation/publish actions.
